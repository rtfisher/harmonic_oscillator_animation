# Claude Code Multi-Agent Demo: Harmonic Oscillator Animation

[![CI](https://github.com/rtfisher/harmonic_oscillator_animation/actions/workflows/ci.yml/badge.svg)](https://github.com/rtfisher/harmonic_oscillator_animation/actions/workflows/ci.yml)

This repository is a talk/demo example showing **Claude Code's multi-agent orchestration capabilities**. A single prompt launched 1 supervisory agent coordinating 7 specialist subagents that collaboratively built, tested, linted, and configured CI/CD for a complete Python desktop application — in ~18 minutes, using ~100K tokens.

## What Was Built

`harmonic_oscillator_animation/` is a PyQt6 desktop application that visualizes undamped simple harmonic motion:

- **Top frame** — animated coil-spring with a hanging mass block, oscillating in real time
- **Bottom frame** — live-updating position-vs-time plot, trace growing rightward from `t=0`

The simulation loops seamlessly every 10 periods. No user controls — purely a demo visualization.

## Files in This Repo

| File | Description |
|------|-------------|
| `claude_interview_prompt.txt` | The seed prompt: asked Claude to interview the user and generate a detailed multi-agent prompt |
| `harmonic_oscillator_animation_prompt.txt` | The resulting full orchestration prompt (fed to Claude Code to build the project) |
| `harmonic_oscillator_animation_run_summary.md` | Run report: timing per subagent, token usage, integration checklist results |
| `setup_env.sh` | Creates the conda environment and installs the package |
| `run.sh` | Launches the app (uses `conda run`; no manual env activation needed) |
| `harmonic_oscillator_animation/` | The generated project (see below) |

## Prerequisites

- [Miniconda](https://docs.conda.io/en/latest/miniconda.html) or Anaconda

## Setup

```bash
bash setup_env.sh
```

## Running the App

```bash
./run.sh
```

## Project Structure

```
harmonic_oscillator_animation/
├── environment.yml          # conda env: python 3.11, numpy, scipy, matplotlib, PyQt6
├── pyproject.toml           # build config, ruff settings, pytest coverage (≥80%)
├── src/
│   └── harmonic_oscillator_animation/
│       ├── physics.py       # SHO constants & functions (position, velocity, acceleration)
│       ├── spring_renderer.py  # SpringRenderer: animated coil + mass matplotlib artist
│       ├── plot_widget.py   # PlotWidget: growing x(t) trace with deque buffer
│       ├── app.py           # MainWindow: QTimer-driven animation loop at ~30 fps
│       └── main.py          # Entry point
└── tests/
    ├── unit/test_physics.py
    ├── integration/test_widgets.py
    └── e2e/test_app.py
```

## Development

```bash
# Lint & format (from harmonic_oscillator_animation/)
ruff check src/ tests/
ruff format src/ tests/

# Tests (requires a display; use xvfb-run on headless Linux)
pytest --tb=short
xvfb-run --auto-servernum pytest   # CI / headless
```

## Physics

Undamped SHO: `x(t) = A·cos(ω₀·t + φ)`

| Constant | Value |
|----------|-------|
| Mass | 1.0 kg |
| Spring constant | 10.0 N/m |
| Amplitude | 0.15 m |
| ω₀ | √(k/m) ≈ 3.162 rad/s |
| Period | 2π/ω₀ ≈ 1.987 s |

## Multi-Agent Build Summary

The project was generated by 1 supervisory agent coordinating 7 specialist subagents:

| Subagent | Task | Time |
|----------|------|------|
| 1 | Environment & Scaffold | 65 s |
| 2 | Physics Engine | 30 s |
| 3 | Spring Renderer | 61 s |
| 4 | Plot Widget | 33 s |
| 5 | Application Shell | 38 s |
| 6 | Test Suite (concurrent with 5) | 62 s |
| 7 | CI/CD Workflow | 28 s |

**Subagent code generation: ~5.5 min wall clock** (22:15:01Z → 22:20:31Z), **~100K tokens**, 16/16 tests passing, 93.5% coverage.

The remaining ~12.5 min was the integration checklist (lint fixes, test run, import verification). Total session including conda env creation (~250 s) and pip downloads: **~18 min wall clock**.

See `harmonic_oscillator_animation_run_summary.md` for full details.
